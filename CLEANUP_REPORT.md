# 🧹 รายการไฟล์และโค้ดที่ไม่จำเป็น

## ไฟล์ที่ควรลบ (ไม่ใช้งานแล้ว)

### 1. ไฟล์ทดสอบ (Test Files)
- ❌ `check_key_format.js` - ไฟล์ตรวจสอบ API key (ใช้แค่ครั้งเดียวตอน setup)
- ❌ `test-connection.js` - ไฟล์ทดสอบการเชื่อมต่อ Supabase (ใช้แค่ครั้งเดียว)

### 2. ไฟล์ข้อมูลเริ่มต้น (Unused Data)
- ❌ `src/data/initialData.js` - ข้อมูล mock ที่ไม่ได้ใช้แล้ว (ใช้ Supabase แทน)

### 3. เอกสารที่ซ้ำซ้อน
- ⚠️ `setup_database.sql` - ซ้ำกับ `reset_database.sql` (เก็บแค่ reset_database.sql)

## โค้ดที่ควรลบ

### 1. Import ที่ไม่ได้ใช้ใน App.jsx
```javascript
// บรรทัด 14 - comment ออกแล้วแต่ยังอยู่
// import { initialRooms, initialBookings, ... } from './data/initialData';
```

### 2. npm dev ที่รันซ้ำ
- มี 2 process ของ `npm run dev` ทำงานพร้อมกัน
- ควร terminate process เก่า (running 25h28m)
- เก็บแค่ process ใหม่ (running 21m)

## สิ่งที่ทำให้ช้า

### 1. ⚠️ Background Images ที่โหลดซ้ำ
ทุก Component ที่มี background image โหลดรูปเดิมซ้ำๆ:
```javascript
backgroundImage: `url(${import.meta.env.BASE_URL}img/background.jpg)`
```
**แนะนำ:** ย้ายไปใช้ CSS class แทน

### 2. ⚠️ Inline Styles มากเกินไป
หลาย Component ใช้ inline styles แทน CSS classes
- ทำให้ React re-render บ่อย
- ควรย้ายไปใช้ CSS classes

### 3. ⚠️ ไม่มี useMemo/useCallback
Component ที่มี filter/search ไม่ได้ใช้ `useMemo` เช่น:
- `ManageBookings.jsx` - filteredBookings
- `ManageEquipment.jsx` - filteredEquipment

## การดำเนินการ

### ขั้นตอนที่ 1: ลบไฟล์ที่ไม่จำเป็น ✅
- [x] ลบ check_key_format.js
- [x] ลบ test-connection.js  
- [x] ลบ src/data/initialData.js
- [x] ลบ setup_database.sql (เก็บ reset_database.sql)

### ขั้นตอนที่ 2: ลบโค้ดที่ไม่ใช้ ✅
- [x] ลบ comment import initialData ใน App.jsx

### ขั้นตอนที่ 3: Optimize Performance (Optional)
- [ ] ย้าย background image ไป CSS
- [ ] เพิ่ม useMemo สำหรับ filtered data
- [ ] ลด inline styles

## ผลลัพธ์ที่คาดหวัง

- ลดขนาดโปรเจค ~10KB
- ลด bundle size
- โค้ดสะอาดขึ้น
- ไม่มีไฟล์ที่ไม่ได้ใช้
