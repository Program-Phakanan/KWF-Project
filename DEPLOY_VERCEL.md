# 🚀 วิธีการ Deploy ขึ้น Vercel

การ Deploy โปรเจคนี้ขึ้น Vercel ทำได้ 2 วิธีหลัก คือ **ผ่าน GitHub (แนะนำ)** หรือ **ผ่าน Vercel CLI**

---

## ✅ วิธีที่ 1: ผ่าน GitHub Integration (แนะนำที่สุด)
วิธีนี้ดีที่สุด เพราะเมื่อคุณแก้โค้ดและ Push ขึ้น GitHub, Vercel จะ Deploy ให้รใหม่อัตโนมัติ

1. **อัพโหลดโค้ดขึ้น GitHub**
   - สร้าง Repository ใหม่ใน GitHub
   - Push โค้ดทั้งหมดขึ้นไป

2. **เชื่อมต่อกับ Vercel**
   - ไปที่ [vercel.com](https://vercel.com) ล็อกอินให้เรียบร้อย
   - กดปุ่ม **"Add New..."** > **"Project"**
   - เลือก **Import** Repository ที่คุณเพิ่งสร้าง

3. **ตั้งค่า Environment Variables (สำคัญมาก!)** ⚠️
   ในหน้า "Configure Project" ก่อนกด Deploy ให้ดูหัวข้อ **Environment Variables**:
   
   ให้เพิ่มข้อมูลจากไฟล์ `.env` ของคุณทีละตัว:
   
   | Name | Value (ตัวอย่าง) |
   |------|------------------|
   | `VITE_SUPABASE_URL` | `https://xxxx.supabase.co` |
   | `VITE_SUPABASE_ANON_KEY` | `eyJhbGciOiJIUzI1NiIsIn...` (Copy ตัวยาวๆ มาให้ครบ) |

4. **กด Deploy**
   - รอสักครู่... เมื่อเสร็จแล้ว Vercel จะให้ URL มา (เช่น `https://meeting-room.vercel.app`)

---

## 🛠 วิธีที่ 2: ผ่าน Vercel CLI (จากเครื่องคุณโดยตรง)
ถ้าขี้เกียจเข้าเว็บ สามารถทำผ่าน Command Line ได้เลย

1. **ติดตั้ง Vercel CLI** (ครั้งแรกครั้งเดียว)
   ```bash
   npm i -g vercel
   ```

2. **Login**
   ```bash
   vercel login
   ```

3. **Deploy**
   รันคำสั่งนี้ในโฟลเดอร์โปรเจค:
   ```bash
   vercel
   ```
   - ตอบคำถามตามที่ขึ้นมา (กด Enter รัวๆ ได้ ส่วนใหญ่ใช้ค่า Default)
   - **Link to existing project?**: No
   - **Link to existing project?**: No
   - **Do you want to modify these settings?**: No

4. **ตั้งค่า Environment Variables**
   เมื่อ Deploy เสร็จครั้งแรก เว็บอาจจะยังใช้ไม่ได้เพราะขาด Key
   - ไปที่ Vercel Dashboard ของโปรเจคที่เพิ่งสร้าง
   - ไปที่ **Settings** > **Environment Variables**
   - เพิ่ม `VITE_SUPABASE_URL` และ `VITE_SUPABASE_ANON_KEY`
   - **สำคัญ:** หลังจากเพิ่มตัวแปรแล้ว ต้องไปที่แท็บ **Deployments** แล้วกด **Redeploy** อีกครั้งเพื่อให้ค่าใหม่มีผล

---

## 💡 คำแนะนำหลัง Deploy

1. **Supabase URL Configuration**
   หมั่นตรวจสอบว่าใน Supabase Dashboard > Authentication > URL Configuration
   คุณได้เพิ่ม URL ของ Vercel (เช่น `https://project-name.vercel.app`) ลงไปใน **Site URL** หรือ **Redirect URLs** แล้ว
   ไม่งั้นเวลา Login อาจจะเด้งผิดที่ได้

2. **ทดสอบระบบ**
   ลองเข้าเว็บจริง แล้วกด F12 ดู Console ว่ามี Error สีแดงหรือไม่ ถ้ามี `401 Unauthorized` ให้เช็ค Environment Variables ใน Vercel อีกรอบ
