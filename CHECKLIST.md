# ✅ Checklist: สิ่งที่ต้องทำก่อนใช้งานระบบ

**อัพเดทล่าสุด**: 26 มกราคม 2026

---

## 🔴 **สิ่งที่ต้องทำทันที** (Critical - ระบบจะไม่ทำงานถ้าไม่ทำ)

### ☐ 1. แก้ไข Supabase API Key ใน `.env`

**สถานะปัจจุบัน**: ❌ API Key ไม่ถูกต้อง

**ทำอะไร**:
```bash
# 1. เปิด Supabase Dashboard
https://app.supabase.com

# 2. เลือกโปรเจค → Settings → API

# 3. คัดลอก "anon public" key (จะยาวมาก ขึ้นต้นด้วย eyJhbGciOi...)

# 4. แก้ไขไฟล์ .env
VITE_SUPABASE_URL=https://yjecazypmkxvfuqiltbk.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOi...

# 5. บันทึกไฟล์

# 6. รีสตาร์ท dev server
# กด Ctrl+C แล้วรัน npm run dev ใหม่
```

**ตรวจสอบ**: Key ต้องยาวกว่า 200 ตัวอักษร และขึ้นต้นด้วย `eyJhbGciOi`

---

### ☐ 2. สร้างตารางในฐานข้อมูล Supabase

**สถานะปัจจุบัน**: ❓ ไม่แน่ใจว่าสร้างแล้วหรือยัง

**ทำอะไร**:
```bash
# 1. เปิด Supabase Dashboard → SQL Editor

# 2. คัดลอก SQL commands จาก SUPABASE_SETUP.md (ขั้นตอนที่ 4)

# 3. รัน SQL เพื่อสร้างตาราง:
#    - rooms
#    - bookings
#    - departments
#    - buildings
#    - equipment

# 4. ตรวจสอบว่าสร้างสำเร็จ → Table Editor
```

**ตรวจสอบ**: ไปที่ **Table Editor** ต้องเห็นตาราง 5 ตาราง

---

### ☐ 3. ตั้งค่า Row Level Security (RLS)

**สถานะปัจจุบัน**: ❓ ไม่แน่ใจว่าตั้งค่าแล้วหรือยัง

**ทำอะไร**:
```bash
# 1. เปิด Supabase Dashboard → SQL Editor

# 2. คัดลอก SQL commands จาก SUPABASE_SETUP.md (ขั้นตอนที่ 5)

# 3. รัน SQL เพื่อ:
#    - เปิดใช้งาน RLS
#    - สร้าง Policies สำหรับอนุญาตให้อ่าน/เขียนข้อมูล

# 4. ตรวจสอบว่าตั้งค่าสำเร็จ
```

**ตรวจสอบ**: ไปที่ **Authentication** → **Policies** ต้องเห็น policies

---

## 🟡 **สิ่งที่ควรทำ** (Recommended - ระบบทำงานได้แต่ไม่มีข้อมูล)

### ☐ 4. เพิ่มข้อมูลตัวอย่าง

**ทำอะไร**:
```bash
# รัน SQL commands จาก SUPABASE_SETUP.md (ขั้นตอนที่ 6)
# เพื่อเพิ่มข้อมูลตัวอย่าง:
#   - แผนก 5 แผนก
#   - อาคาร 3 อาคาร
#   - อุปกรณ์ 6 รายการ
#   - ห้องประชุม 3 ห้อง
```

**ตรวจสอบ**: เปิดเว็บแล้วต้องเห็นห้องประชุมแสดงในหน้าแรก

---

### ☐ 5. สร้างผู้ใช้ Admin (ถ้าต้องการ login ด้วย Supabase)

**ทำอะไร**:
```bash
# 1. เปิด Supabase Dashboard → Authentication → Users

# 2. คลิก "Add user"

# 3. กรอก:
#    Email: admin@example.com
#    Password: admin123456

# 4. คลิก "Create user"
```

**หมายเหตุ**: ถ้าไม่สร้าง ยังใช้ fallback login ได้ (admin/admin)

---

## 🟢 **สิ่งที่ทำเสร็จแล้ว** (Completed)

### ✅ 1. Supabase Client Configuration
- แก้ไข `src/lib/supabaseClient.js` ให้ใช้ env variables
- เพิ่ม options: autoRefreshToken, persistSession

### ✅ 2. Authentication Integration
- แก้ไข `LoginPage.jsx` ให้ใช้ Supabase Auth
- มี fallback เป็น hardcoded credentials

### ✅ 3. Session Persistence
- เพิ่มการตรวจสอบ session ใน `App.jsx`
- ติดตาม auth state changes

### ✅ 4. Logout Functionality
- แก้ไข `Navigation.jsx` ให้ใช้ Supabase signOut

### ✅ 5. Error Handling
- สร้าง `ErrorBoundary.jsx`
- เพิ่ม ErrorBoundary wrapper ใน `main.jsx`

### ✅ 6. Documentation
- สร้าง `SUPABASE_SETUP.md` - คู่มือตั้งค่า
- สร้าง `INSPECTION_REPORT.md` - รายงานการตรวจสอบ
- สร้าง `CHECKLIST.md` - เอกสารนี้

---

## 🧪 การทดสอบ

### ขั้นตอนการทดสอบหลังทำครบทุกข้อ:

```bash
# 1. รีสตาร์ท dev server
npm run dev

# 2. เปิด browser ไปที่
http://localhost:5173

# 3. เปิด Console (F12)

# 4. ตรวจสอบ:
#    ✅ ไม่มี error ใน console
#    ✅ เห็นห้องประชุมแสดงในหน้าแรก
#    ✅ สามารถ login ได้ (admin/admin หรือ admin@example.com)
#    ✅ สามารถจองห้องได้
#    ✅ สามารถดูข้อมูลใน Admin Dashboard ได้

# 5. ทดสอบ Reload
#    กด F5 → ต้องยัง login อยู่ (ไม่ต้อง login ใหม่)
```

---

## 📊 สถานะความพร้อม

| หมวดหมู่ | สถานะ | หมายเหตุ |
|---------|-------|----------|
| โค้ด | ✅ 100% | พร้อมใช้งาน |
| Supabase Config | ⚠️ 50% | ต้องแก้ไข API Key |
| Database Schema | ❓ 0% | ต้องสร้างตาราง |
| RLS Policies | ❓ 0% | ต้องตั้งค่า |
| ข้อมูลตัวอย่าง | ❓ 0% | ควรเพิ่ม |
| Error Handling | ✅ 100% | เพิ่ม ErrorBoundary แล้ว |

**ความพร้อมรวม**: ⚠️ **50%** - ต้องทำ 3 ข้อแรกก่อนใช้งาน

---

## 🚀 Quick Start (สำหรับคนรีบ)

```bash
# 1. แก้ไข .env (ใส่ API Key ที่ถูกต้อง)

# 2. เปิด Supabase Dashboard → SQL Editor

# 3. รัน SQL จาก SUPABASE_SETUP.md:
#    - ขั้นตอนที่ 4 (สร้างตาราง)
#    - ขั้นตอนที่ 5 (ตั้งค่า RLS)
#    - ขั้นตอนที่ 6 (เพิ่มข้อมูลตัวอย่าง)

# 4. รีสตาร์ท dev server
npm run dev

# 5. เปิด http://localhost:5173
```

---

## ❓ ถ้ามีปัญหา

1. ✅ อ่าน `INSPECTION_REPORT.md` เพื่อดูรายละเอียด
2. ✅ อ่าน `SUPABASE_SETUP.md` เพื่อดูวิธีแก้ไข
3. ✅ เปิด Browser Console (F12) เพื่อดู error
4. ✅ ตรวจสอบว่า dev server รันอยู่

---

**สร้างโดย**: Antigravity AI Assistant  
**วันที่**: 26 มกราคม 2026
