# 🖼️ แก้ปัญหา: รูปภาพหายหลัง Refresh

## 🔍 สาเหตุของปัญหา

เดิมระบบเก็บรูปเป็น **Base64 string** ซึ่ง:
- ❌ เก็บใน memory เท่านั้น (หายหลัง Refresh)
- ❌ ขนาดใหญ่มาก (ไม่เหมาะกับ Database)
- ❌ ช้าในการโหลด

## ✅ วิธีแก้ไข

ใช้ **Supabase Storage** เก็บรูปภาพแทน:
- ✅ อัพโหลดรูปไปเก็บที่ Supabase Storage
- ✅ เก็บแค่ URL ใน Database
- ✅ รูปไม่หายหลัง Refresh
- ✅ โหลดเร็วขึ้น

---

## 📋 ขั้นตอนการตั้งค่า

### ขั้นตอนที่ 1: สร้าง Storage Bucket

1. เข้า **Supabase Dashboard** → [https://supabase.com](https://supabase.com)
2. เลือกโปรเจคของคุณ
3. คลิกเมนู **Storage** (ด้านซ้าย)
4. คลิกปุ่ม **Create a new bucket**
5. กรอกข้อมูล:
   - **Name:** `room-images`
   - **Public bucket:** ✅ เลือก (เพื่อให้เข้าถึงรูปได้โดยไม่ต้อง login)
6. คลิก **Create bucket**

### ขั้นตอนที่ 2: ตั้งค่า Storage Policies

1. คลิกที่ bucket **room-images** ที่เพิ่งสร้าง
2. ไปที่แท็บ **Policies**
3. คลิก **New policy**
4. เลือก **For full customization**
5. วางโค้ดจากไฟล์ `sql/04_setup_storage.sql`

**หรือ** รันผ่าน SQL Editor:

1. ไปที่ **SQL Editor**
2. คลิก **+ New query**
3. เปิดไฟล์ `sql/04_setup_storage.sql`
4. คัดลอกโค้ดทั้งหมด → วางใน SQL Editor
5. คลิก **Run**
6. รอจนเห็น **Success**

### ขั้นตอนที่ 3: ทดสอบระบบ

1. กลับมาที่เว็บไซต์ `http://localhost:5173`
2. Login ด้วย `admin` / `admin`
3. ไปที่เมนู **จัดการห้องประชุม**
4. คลิก **เพิ่มห้องประชุม**
5. กรอกข้อมูล:
   - ชื่อห้อง: `ห้องทดสอบ`
   - อาคาร: `อาคาร A`
   - ชั้น: `1`
   - ความจุ: `10`
6. **อัพโหลดรูปภาพ** (คลิกที่กล่องอัพโหลด)
7. คลิก **บันทึก**
8. ✅ ควรเห็นข้อความ "กำลังอัพโหลดรูปภาพ..." แล้วตามด้วย "เพิ่มห้องประชุมเรียบร้อย!"
9. **Refresh หน้า (F5)**
10. ✅ **รูปภาพควรยังแสดงอยู่!**

---

## 🔍 ตรวจสอบว่ารูปถูกอัพโหลดจริง

### วิธีที่ 1: ตรวจสอบใน Supabase Storage

1. เข้า Supabase Dashboard
2. ไปที่ **Storage** → เลือก bucket **room-images**
3. ✅ ควรเห็นไฟล์รูปภาพที่อัพโหลด

### วิธีที่ 2: ตรวจสอบใน Table Editor

1. ไปที่ **Table Editor** → เลือกตาราง `rooms`
2. ดูคอลัมน์ `image_url`
3. ✅ ควรเห็น URL แบบนี้:
   ```
   https://yjecazypmkxvfuqiltbk.supabase.co/storage/v1/object/public/room-images/1738048123456_abc123.jpg
   ```

### วิธีที่ 3: ตรวจสอบใน Browser Console

1. เปิด Developer Tools (F12)
2. ไปที่แท็บ **Console**
3. อัพโหลดรูปใหม่
4. ดูข้อความ:
   - ✅ `Upload error:` = มี error
   - ✅ ไม่มี error = อัพโหลดสำเร็จ

---

## 🚨 ถ้ายังมีปัญหา

### ปัญหา: "Failed to upload image"

**สาเหตุ:** Storage bucket ยังไม่ถูกสร้าง หรือ Policies ยังไม่ถูกตั้งค่า

**วิธีแก้:**
1. ตรวจสอบว่ามี bucket `room-images` หรือยัง
2. ตรวจสอบว่า bucket เป็น **Public** หรือไม่
3. รัน SQL script `sql/04_setup_storage.sql` อีกครั้ง

### ปัญหา: "Policy violation"

**สาเหตุ:** Storage Policies ยังไม่ถูกตั้งค่า

**วิธีแก้:**
1. รัน SQL script `sql/04_setup_storage.sql`
2. ตรวจสอบว่า Policies ถูกสร้างแล้ว:
   - ไปที่ **Storage** → เลือก bucket **room-images** → แท็บ **Policies**
   - ควรเห็น 4 policies: Public Access, Public Upload, Public Delete, Public Update

### ปัญหา: "Bucket not found"

**สาเหตุ:** ชื่อ bucket ไม่ตรงกัน

**วิธีแก้:**
1. ตรวจสอบว่าสร้าง bucket ชื่อ `room-images` (ไม่ใช่ชื่ออื่น)
2. ถ้าสร้างชื่ออื่น ให้แก้ไขในไฟล์ `src/utils/storage.js`:
   ```javascript
   export async function uploadImage(file, bucket = 'ชื่อ-bucket-ของคุณ') {
   ```

---

## 📊 สรุปการเปลี่ยนแปลง

### ไฟล์ที่สร้างใหม่:

1. **`src/utils/storage.js`** - ฟังก์ชันสำหรับอัพโหลด/ลบรูปภาพ
2. **`sql/04_setup_storage.sql`** - SQL script สำหรับตั้งค่า Storage Policies

### ไฟล์ที่แก้ไข:

1. **`src/components/ManageRooms.jsx`**
   - เพิ่ม `import { uploadImage, deleteImage }`
   - แก้ไข `handleImageUpload` - เก็บไฟล์แทน base64
   - แก้ไข `handleSaveRoom` - อัพโหลดรูปไปที่ Storage
   - แก้ไข `confirmDelete` - ลบรูปจาก Storage ด้วย

### การทำงานใหม่:

**เดิม:**
```
อัพโหลดรูป → แปลงเป็น Base64 → เก็บใน memory → หายหลัง Refresh
```

**ใหม่:**
```
อัพโหลดรูป → อัพโหลดไปที่ Supabase Storage → ได้ URL → เก็บ URL ใน Database → ไม่หายหลัง Refresh
```

---

## ✅ ผลลัพธ์ที่คาดหวัง

หลังจากตั้งค่าเสร็จแล้ว:

1. ✅ อัพโหลดรูปภาพได้
2. ✅ รูปภาพถูกเก็บที่ Supabase Storage
3. ✅ Refresh หน้าแล้วรูปยังแสดงอยู่
4. ✅ แก้ไขรูปได้ (รูปเก่าจะถูกลบอัตโนมัติ)
5. ✅ ลบห้องแล้วรูปจะถูกลบด้วย
6. ✅ โหลดรูปเร็วขึ้น

---

## 📌 หมายเหตุ

- ขนาดไฟล์สูงสุด: **5 MB**
- รองรับไฟล์: **JPG, PNG, GIF, WebP**
- รูปภาพจะถูกเก็บแบบ Public (ทุกคนเข้าถึงได้)
- ชื่อไฟล์จะถูกสร้างแบบสุ่ม (ไม่ซ้ำกัน)

---

**หมายเหตุ:** คุณ**ต้องสร้าง Storage Bucket ใน Supabase Dashboard** ก่อน ระบบถึงจะอัพโหลดรูปได้! 🚀
