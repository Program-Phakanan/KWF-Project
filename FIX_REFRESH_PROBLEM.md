# 🔧 วิธีแก้ปัญหา: Refresh แล้วข้อมูลหาย

## 🔍 สาเหตุของปัญหา

จากการตรวจสอบพบว่า:

1. ✅ **โค้ดเชื่อมต่อ Database ถูกต้องแล้ว** - ทุก Component ใช้ `insertData`, `updateData`, `deleteData`
2. ✅ **ระบบโหลดข้อมูลจาก Supabase** - มีโค้ดใน `App.jsx` (บรรทัด 76-123)
3. ❌ **แต่ Database ยังไม่มีข้อมูล** - ยังไม่ได้รัน SQL script

## 📋 ขั้นตอนการแก้ไข

### ขั้นตอนที่ 1: เข้า Supabase Dashboard

1. เปิดเบราว์เซอร์ไปที่ [https://supabase.com](https://supabase.com)
2. Login เข้าสู่ระบบ
3. เลือกโปรเจค **meeting-room-booking** (หรือชื่อที่คุณตั้ง)

### ขั้นตอนที่ 2: รัน SQL Script

1. คลิกที่เมนู **SQL Editor** (ด้านซ้าย)
2. คลิกปุ่ม **+ New query**
3. คัดลอกโค้ดจากไฟล์ `reset_database.sql` ทั้งหมด
4. วางลงในช่อง SQL Editor
5. คลิกปุ่ม **Run** (หรือกด Ctrl+Enter)
6. รอจนกว่าจะเห็นข้อความ **Success** สีเขียว

### ขั้นตอนที่ 3: ตรวจสอบข้อมูล

1. คลิกที่เมนู **Table Editor** (ด้านซ้าย)
2. เลือกตาราง `rooms` - ควรเห็น 5 ห้องประชุม
3. เลือกตาราง `departments` - ควรเห็น 5 แผนก
4. เลือกตาราง `equipment` - ควรเห็น 6 อุปกรณ์
5. เลือกตาราง `buildings` - ควรเห็น 3 อาคาร
6. เลือกตาราง `bookings` - ควรเห็น 3 การจอง

### ขั้นตอนที่ 4: ทดสอบระบบ

1. กลับมาที่เว็บไซต์ `http://localhost:5173`
2. **Refresh หน้า (F5)**
3. ตอนนี้ควรเห็นข้อมูลห้องประชุมแล้ว!
4. ลองเพิ่มอุปกรณ์ใหม่
5. **Refresh หน้าอีกครั้ง**
6. ข้อมูลที่เพิ่มควรยังอยู่!

---

## 🎯 สิ่งที่แก้ไขใน SQL Script

### 1. เพิ่มฟิลด์ที่ขาดหายไป

#### ตาราง `departments`
```sql
CREATE TABLE departments (
  id SERIAL PRIMARY KEY,
  name VARCHAR(255) NOT NULL,
  organization VARCHAR(255),  -- ✅ เพิ่มฟิลด์นี้
  created_at TIMESTAMP DEFAULT NOW()
);
```

#### ตาราง `equipment`
```sql
CREATE TABLE equipment (
  id SERIAL PRIMARY KEY,
  name VARCHAR(255) NOT NULL,
  quantity INTEGER DEFAULT 0,           -- ✅ เพิ่มฟิลด์นี้
  category VARCHAR(255),                -- ✅ เพิ่มฟิลด์นี้
  status VARCHAR(255) DEFAULT 'พร้อมใช้งาน',  -- ✅ เพิ่มฟิลด์นี้
  created_at TIMESTAMP DEFAULT NOW()
);
```

#### ตาราง `bookings`
```sql
CREATE TABLE bookings (
  ...
  status VARCHAR(50) DEFAULT 'pending',  -- ✅ เพิ่มฟิลด์นี้
  ...
);
```

### 2. เพิ่มข้อมูลตัวอย่างที่สมบูรณ์

```sql
-- แผนก (มี organization)
INSERT INTO departments (name, organization) VALUES 
  ('ฝ่ายบริหาร', 'บริษัท ABC จำกัด'),
  ...

-- อุปกรณ์ (มี quantity, category, status)
INSERT INTO equipment (name, quantity, category, status) VALUES 
  ('โปรเจคเตอร์', 5, 'เครื่องฉาย', 'พร้อมใช้งาน'),
  ...

-- ห้องประชุม (มี image_url)
INSERT INTO rooms (name, capacity, building, floor, equipment, image_url) VALUES 
  ('ห้องประชุมใหญ่', 50, 'อาคาร A', 3, ARRAY['โปรเจคเตอร์', 'ระบบเสียง'], '/img/room1.jpg'),
  ...

-- การจอง (มี status)
INSERT INTO bookings (..., status) VALUES 
  (..., 'confirmed'),
  ...
```

---

## ✅ ผลลัพธ์ที่คาดหวัง

หลังจากรัน SQL script แล้ว:

1. ✅ เว็บไซต์จะแสดงห้องประชุม 5 ห้อง
2. ✅ เมนูจัดการจะมีข้อมูลเริ่มต้น
3. ✅ เพิ่มข้อมูลใหม่แล้ว Refresh จะไม่หาย
4. ✅ แก้ไขข้อมูลแล้ว Refresh จะถูกบันทึก
5. ✅ ลบข้อมูลแล้ว Refresh จะถูกลบจริง

---

## 🔍 การตรวจสอบเพิ่มเติม

### ตรวจสอบผ่าน Browser Console

1. เปิด Developer Tools (F12)
2. ไปที่แท็บ **Console**
3. ดูข้อความ:
   - ✅ `✅ Found existing session:` = Login สำเร็จ
   - ✅ ไม่มี error สีแดง = โหลดข้อมูลสำเร็จ
   - ❌ `Error loading rooms:` = มีปัญหา

### ตรวจสอบผ่าน Network Tab

1. เปิด Developer Tools (F12)
2. ไปที่แท็บ **Network**
3. Refresh หน้า
4. ดูคำขอ (requests) ไปยัง Supabase:
   - ✅ Status 200 = สำเร็จ
   - ❌ Status 400/401/403 = มีปัญหา

---

## 🚨 ถ้ายังมีปัญหา

### ปัญหา: "Invalid login credentials"

**วิธีแก้:**
1. ไปที่ Supabase Dashboard → **Authentication** → **Users**
2. คลิก **Add user** → **Create new user**
3. กรอก:
   - Email: `admin@example.com`
   - Password: `admin123456`
4. คลิก **Create user**
5. กลับมา Login ใหม่

### ปัญหา: "Failed to fetch"

**วิธีแก้:**
1. ตรวจสอบไฟล์ `.env`:
   ```
   VITE_SUPABASE_URL=https://yjecazypmkxvfuqiltbk.supabase.co
   VITE_SUPABASE_ANON_KEY=eyJhbGci...
   ```
2. **Restart dev server:**
   ```bash
   # กด Ctrl+C เพื่อหยุด
   npm run dev
   ```

### ปัญหา: "Row Level Security policy violation"

**วิธีแก้:**
1. ตรวจสอบว่ารัน SQL script ครบทุกบรรทัด
2. ตรวจสอบว่า RLS Policies ถูกสร้างแล้ว
3. ถ้ายังไม่ได้ ให้รัน SQL script ใหม่อีกครั้ง

---

## 📌 สรุป

**ปัญหา:** ข้อมูลหายหลัง Refresh  
**สาเหตุ:** Database ยังไม่มีข้อมูล  
**วิธีแก้:** รัน `reset_database.sql` ใน Supabase SQL Editor  
**ผลลัพธ์:** ข้อมูลจะถูกบันทึกและไม่หายหลัง Refresh  

---

**หมายเหตุ:** หลังจากรัน SQL script แล้ว ระบบจะพร้อมใช้งานเต็มรูปแบบ ข้อมูลทั้งหมดจะถูกบันทึกลง Supabase และจะไม่หายหลัง Refresh หน้า! 🎉
