# 📤 คู่มือการ Upload โปรเจคขึ้น GitHub

## 📋 สารบัญ
1. [ติดตั้ง Git](#1-ติดตั้ง-git)
2. [ตั้งค่า Git](#2-ตั้งค่า-git)
3. [เตรียมโปรเจค](#3-เตรียมโปรเจค)
4. [Upload ขึ้น GitHub](#4-upload-ขึ้น-github)
5. [ตรวจสอบผลลัพธ์](#5-ตรวจสอบผลลัพธ์)

---

## 1. ติดตั้ง Git

### สำหรับ Windows

#### ขั้นตอนที่ 1: ดาวน์โหลด Git

1. เปิดเบราว์เซอร์ไปที่: **https://git-scm.com/download/win**
2. ดาวน์โหลดไฟล์ติดตั้ง (64-bit Git for Windows Setup)
3. รอให้ดาวน์โหลดเสร็จ

#### ขั้นตอนที่ 2: ติดตั้ง Git

1. เปิดไฟล์ที่ดาวน์โหลด (เช่น `Git-2.43.0-64-bit.exe`)
2. คลิก **Next** ในหน้าต้อนรับ
3. เลือกโฟลเดอร์ติดตั้ง (ใช้ค่าเริ่มต้น) → **Next**
4. เลือก Components (ใช้ค่าเริ่มต้น) → **Next**
5. เลือก Start Menu folder → **Next**
6. **สำคัญ!** เลือก Default editor:
   - แนะนำ: **Use Visual Studio Code as Git's default editor**
   - หรือ: **Use Notepad as Git's default editor**
   - คลิก **Next**
7. เลือก "Let Git decide" → **Next**
8. **สำคัญ!** เลือก "Git from the command line and also from 3rd-party software" → **Next**
9. เลือก "Use bundled OpenSSH" → **Next**
10. เลือก "Use the OpenSSL library" → **Next**
11. เลือก "Checkout Windows-style, commit Unix-style line endings" → **Next**
12. เลือก "Use MinTTY" → **Next**
13. เลือก "Default (fast-forward or merge)" → **Next**
14. เลือก "Git Credential Manager" → **Next**
15. เลือก "Enable file system caching" → **Next**
16. ไม่ต้องเลือก experimental features → **Install**
17. รอให้ติดตั้งเสร็จ
18. คลิก **Finish**

#### ขั้นตอนที่ 3: ตรวจสอบการติดตั้ง

1. **รีสตาร์ท PowerShell หรือ Terminal** (สำคัญมาก!)
2. เปิด PowerShell ใหม่
3. พิมพ์คำสั่ง:

```powershell
git --version
```

4. ควรเห็นผลลัพธ์เช่น: `git version 2.43.0.windows.1`
5. ✅ ถ้าเห็นเลข version แสดงว่าติดตั้งสำเร็จ!

---

## 2. ตั้งค่า Git

### ตั้งค่าชื่อและอีเมล (ครั้งแรกเท่านั้น)

เปิด PowerShell และพิมพ์:

```powershell
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"
```

**ตัวอย่าง:**
```powershell
git config --global user.name "Phakanan"
git config --global user.email "phakanan@example.com"
```

### ตรวจสอบการตั้งค่า

```powershell
git config --global --list
```

ควรเห็น:
```
user.name=Your Name
user.email=your.email@example.com
```

---

## 3. เตรียมโปรเจค

### ตรวจสอบไฟล์ .gitignore

ไฟล์ `.gitignore` ควรมีเนื้อหาดังนี้:

```gitignore
# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.vercel

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
```

✅ **สำคัญ:** ต้องมี `.env` และ `node_modules` ใน `.gitignore`

### ตรวจสอบไฟล์ .env.example

ตรวจสอบว่ามีไฟล์ `.env.example`:

```env
# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
```

---

## 4. Upload ขึ้น GitHub

### ขั้นตอนที่ 1: เข้าไปยังโฟลเดอร์โปรเจค

```powershell
cd c:\Users\LENOVO\Desktop\Meeting-room
```

### ขั้นตอนที่ 2: สร้าง Git Repository

```powershell
git init
```

ควรเห็น: `Initialized empty Git repository in ...`

### ขั้นตอนที่ 3: เพิ่มไฟล์ทั้งหมด

```powershell
git add .
```

**หมายเหตุ:** คำสั่งนี้จะเพิ่มไฟล์ทั้งหมด ยกเว้นไฟล์ที่อยู่ใน `.gitignore`

### ขั้นตอนที่ 4: ตรวจสอบไฟล์ที่จะ Upload

```powershell
git status
```

ควรเห็นรายการไฟล์สีเขียว (ไฟล์ที่จะ upload)

**ตรวจสอบว่า:**
- ❌ **ไม่มี** `.env` (ต้องไม่ upload)
- ❌ **ไม่มี** `node_modules/` (ต้องไม่ upload)
- ✅ **มี** `.env.example` (ควร upload)
- ✅ **มี** `src/`, `public/`, `sql/` (ควร upload)

### ขั้นตอนที่ 5: Commit การเปลี่ยนแปลง

```powershell
git commit -m "Initial commit: Meeting Room Booking System with Supabase"
```

ควรเห็น: `[main (root-commit) ...] Initial commit: ...`

### ขั้นตอนที่ 6: เปลี่ยนชื่อ Branch เป็น main

```powershell
git branch -M main
```

### ขั้นตอนที่ 7: เชื่อมต่อกับ GitHub Repository

```powershell
git remote add origin https://github.com/Program-Phakanan/KWF-Project.git
```

### ขั้นตอนที่ 8: Push ขึ้น GitHub

#### กรณีที่ 1: Repository ว่างเปล่า (แนะนำ)

```powershell
git push -u origin main
```

#### กรณีที่ 2: Repository มีข้อมูลอยู่แล้ว

```powershell
git push -u origin main --force
```

**⚠️ คำเตือน:** `--force` จะเขียนทับข้อมูลเดิมใน GitHub

### ขั้นตอนที่ 9: Login GitHub (ถ้าถูกถาม)

1. เบราว์เซอร์จะเปิดขึ้นมา
2. Login เข้า GitHub
3. อนุญาตให้ Git เข้าถึง
4. กลับมาที่ PowerShell
5. รอให้ Upload เสร็จ

---

## 5. ตรวจสอบผลลัพธ์

### ตรวจสอบบน GitHub

1. เปิดเบราว์เซอร์ไปที่: **https://github.com/Program-Phakanan/KWF-Project**
2. ควรเห็นไฟล์ทั้งหมด:
   - ✅ `src/`
   - ✅ `public/`
   - ✅ `sql/`
   - ✅ `README.md`
   - ✅ `.env.example`
   - ✅ `package.json`
   - ❌ **ไม่มี** `.env`
   - ❌ **ไม่มี** `node_modules/`

### ตรวจสอบ README

1. คลิกที่ `README.md`
2. ควรเห็นเนื้อหาที่เขียนไว้
3. ตรวจสอบว่าแสดงผลถูกต้อง

---

## 🔄 การอัพเดทโค้ดในอนาคต

### เมื่อแก้ไขโค้ด

```powershell
# 1. เพิ่มไฟล์ที่แก้ไข
git add .

# 2. Commit พร้อมข้อความ
git commit -m "อธิบายการเปลี่ยนแปลง"

# 3. Push ขึ้น GitHub
git push
```

**ตัวอย่าง:**
```powershell
git add .
git commit -m "แก้ไข: ปรับปรุง UI หน้า Login"
git push
```

### ตรวจสอบสถานะ

```powershell
# ดูไฟล์ที่เปลี่ยนแปลง
git status

# ดูประวัติ commit
git log --oneline

# ดู remote repository
git remote -v
```

---

## 🚨 การแก้ปัญหา

### ปัญหา: "git: command not found"

**สาเหตุ:** Git ยังไม่ได้ติดตั้งหรือยังไม่ได้รีสตาร์ท Terminal

**วิธีแก้:**
1. ตรวจสอบว่าติดตั้ง Git แล้ว
2. **รีสตาร์ท PowerShell** (สำคัญมาก!)
3. ลองพิมพ์ `git --version` อีกครั้ง

### ปัญหา: "Permission denied"

**สาเหตุ:** ไม่มีสิทธิ์เข้าถึง GitHub Repository

**วิธีแก้:**
1. ตรวจสอบว่า Login GitHub แล้ว
2. ตรวจสอบว่า URL ถูกต้อง
3. ลอง Login ใหม่:
   ```powershell
   git credential-manager-core erase https://github.com
   git push
   ```

### ปัญหา: "Failed to push"

**สาเหตุ:** Repository มีข้อมูลที่ไม่ตรงกัน

**วิธีแก้:**
```powershell
# ดึงข้อมูลล่าสุดจาก GitHub
git pull origin main --allow-unrelated-histories

# แก้ไข conflicts (ถ้ามี)

# Push อีกครั้ง
git push -u origin main
```

### ปัญหา: ".env ถูก upload ขึ้น GitHub"

**วิธีแก้:**
```powershell
# ลบไฟล์ออกจาก Git (แต่ไม่ลบจากเครื่อง)
git rm --cached .env

# Commit
git commit -m "Remove .env from repository"

# Push
git push
```

---

## 📌 Checklist ก่อน Upload

- [ ] ตรวจสอบว่า `.env` อยู่ใน `.gitignore`
- [ ] ตรวจสอบว่า `node_modules` อยู่ใน `.gitignore`
- [ ] มีไฟล์ `.env.example` สำหรับคนอื่นใช้อ้างอิง
- [ ] มีไฟล์ `README.md` อธิบายโปรเจค
- [ ] ลบ API keys, passwords ออกจากโค้ด
- [ ] ทดสอบว่าโค้ดทำงานได้ก่อน upload
- [ ] เขียน commit message ที่ชัดเจน

---

## 🎯 สรุป

### คำสั่งที่ใช้บ่อย

```powershell
# ติดตั้ง Git (ครั้งแรก)
# ดาวน์โหลดจาก https://git-scm.com/download/win

# ตั้งค่า Git (ครั้งแรก)
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"

# สร้าง Repository (ครั้งแรก)
cd c:\Users\LENOVO\Desktop\Meeting-room
git init
git add .
git commit -m "Initial commit: Meeting Room Booking System"
git branch -M main
git remote add origin https://github.com/Program-Phakanan/KWF-Project.git
git push -u origin main

# อัพเดทโค้ด (ครั้งต่อไป)
git add .
git commit -m "อธิบายการเปลี่ยนแปลง"
git push
```

---

**หมายเหตุ:** ถ้ามีปัญหาหรือข้อสงสัย สามารถดูเอกสารเพิ่มเติมได้ที่:
- [Git Documentation](https://git-scm.com/doc)
- [GitHub Guides](https://guides.github.com/)

**วันที่อัพเดท:** 2026-01-28  
**สถานะ:** ✅ พร้อม Upload GitHub
