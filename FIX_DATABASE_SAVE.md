# 🔴 ปัญหา: ข้อมูลไม่ถูกบันทึกลง Supabase

## สาเหตุ
ทุก Component ที่จัดการข้อมูล (CRUD) **ไม่ได้เชื่อมต่อกับ Supabase**
- เปลี่ยนแค่ state ใน memory
- พอ refresh หน้าเว็บ ข้อมูลหายหมด

## ไฟล์ที่มีปัญหา

### ✅ แก้ไขแล้ว
1. **ManageRooms.jsx** - เพิ่ม/แก้ไข/ลบห้องประชุม ✅ แก้แล้ว
2. **RoomDetail.jsx** - จองห้อง ✅ แก้แล้ว (มีอยู่แล้ว)

### ❌ ยังไม่แก้
3. **ManageBookings.jsx** - แก้ไข/ลบการจอง
4. **ManageDepartments.jsx** - เพิ่ม/แก้ไข/ลบแผนก
5. **ManageBuildings.jsx** - เพิ่ม/แก้ไข/ลบอาคาร
6. **ManageEquipment.jsx** - เพิ่ม/แก้ไข/ลบอุปกรณ์

## วิธีแก้ไข

### สำหรับแต่ละไฟล์ ต้องทำ 3 ขั้นตอน:

#### 1. Import database functions
```javascript
import { insertData, updateData, deleteData } from '../utils/database';
```

#### 2. แก้ไข handleSave ให้เป็น async และเรียก Supabase
```javascript
const handleSave = async () => {
  // validation...
  
  try {
    if (isEditing) {
      // Update in Supabase
      const { error } = await updateData('table_name', data, { id: item.id });
      if (error) throw error;
    } else {
      // Insert to Supabase
      const { data: newData, error } = await insertData('table_name', data);
      if (error) throw error;
    }
    
    // Update local state
    // ...
  } catch (error) {
    showAlert('error', 'เกิดข้อผิดพลาด: ' + error.message);
  }
};
```

#### 3. แก้ไข confirmDelete ให้เป็น async และเรียก Supabase
```javascript
const confirmDelete = async () => {
  try {
    const { error } = await deleteData('table_name', { id: itemId });
    if (error) throw error;
    
    // Update local state
    // ...
  } catch (error) {
    showAlert('error', 'เกิดข้อผิดพลาด: ' + error.message);
  }
};
```

## ตารางที่ใช้ในแต่ละไฟล์

| ไฟล์ | ตาราง Supabase | ฟิลด์ที่ต้องแปลง |
|------|----------------|------------------|
| ManageRooms.jsx | `rooms` | `image` → `image_url`, `equipment` → array |
| ManageBookings.jsx | `bookings` | `roomId` → `room_id`, `startTime` → `start_time`, etc. |
| ManageDepartments.jsx | `departments` | ไม่ต้องแปลง |
| ManageBuildings.jsx | `buildings` | ไม่ต้องแปลง |
| ManageEquipment.jsx | `equipment` | ไม่ต้องแปลง |

## สถานะการแก้ไข

- [x] ManageRooms.jsx - เพิ่ม insertData, updateData, deleteData ✅
- [x] RoomDetail.jsx - มีอยู่แล้ว ✅
- [x] ManageBookings.jsx - แก้ไขเรียบร้อย ✅
- [x] ManageDepartments.jsx - แก้ไขเรียบร้อย ✅
- [x] ManageBuildings.jsx - แก้ไขเรียบร้อย ✅
- [x] ManageEquipment.jsx - แก้ไขเรียบร้อย ✅

## ✅ เสร็จสิ้น!

**ทุกไฟล์ได้รับการแก้ไขแล้ว** ตอนนี้ระบบจะบันทึกข้อมูลลง Supabase จริงๆ

### ขั้นตอนถัดไป:

1. **ตรวจสอบว่า Database ถูก Setup แล้ว**
   - เปิด Supabase Dashboard
   - ไปที่ SQL Editor
   - Run `reset_database.sql` หรือ `setup_database.sql`

2. **ทดสอบระบบ**
   - เพิ่มห้องประชุม → Refresh → ควรยังมีข้อมูล ✅
   - จองห้อง → Refresh → ควรยังมีข้อมูล ✅
   - เพิ่มแผนก/อาคาร/อุปกรณ์ → Refresh → ควรยังมีข้อมูล ✅

3. **ตรวจสอบ Console**
   - กด F12 เปิด Developer Tools
   - ดู Console Tab
   - ถ้ามี error ให้แจ้งเพื่อแก้ไข
