# ✅ Checklist - Upload โปรเจคขึ้น GitHub

## 📋 ขั้นตอนที่ต้องทำ

### ขั้นตอนที่ 1: เตรียมความพร้อม

- [ ] อ่าน **FIX_SUMMARY.md** เพื่อเข้าใจว่าระบบทำงานอย่างไร
- [ ] อ่าน **QUICK_START.md** เพื่อดูคำสั่งย่อ
- [ ] เตรียม GitHub Account (ถ้ายังไม่มี: https://github.com/signup)

---

### ขั้นตอนที่ 2: ติดตั้ง Git

- [ ] ดาวน์โหลด Git จาก: **https://git-scm.com/download/win**
- [ ] รันไฟล์ติดตั้ง
- [ ] เลือก "Git from the command line and also from 3rd-party software"
- [ ] ติดตั้งจนเสร็จ
- [ ] **รีสตาร์ท PowerShell** (สำคัญมาก!)
- [ ] ตรวจสอบ: `git --version`
- [ ] ✅ เห็นเลข version = ติดตั้งสำเร็จ

---

### ขั้นตอนที่ 3: ตั้งค่า Git (ครั้งแรกเท่านั้น)

เปิด PowerShell และพิมพ์:

```powershell
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"
```

**แทนที่:**
- `Your Name` = ชื่อของคุณ
- `your.email@example.com` = อีเมลของคุณ

**ตัวอย่าง:**
```powershell
git config --global user.name "Phakanan"
git config --global user.email "phakanan@example.com"
```

**ตรวจสอบ:**
- [ ] พิมพ์: `git config --global --list`
- [ ] ✅ เห็น user.name และ user.email

---

### ขั้นตอนที่ 4: ตรวจสอบไฟล์

- [ ] เปิดไฟล์ `.gitignore`
- [ ] ✅ มี `.env` ในรายการ
- [ ] ✅ มี `node_modules` ในรายการ
- [ ] ✅ มี `.vercel` ในรายการ

- [ ] เปิดไฟล์ `.env.example`
- [ ] ✅ มีตัวอย่าง environment variables

- [ ] เปิดไฟล์ `README.md`
- [ ] ✅ มีเนื้อหาอธิบายโปรเจค

---

### ขั้นตอนที่ 5: สร้าง Git Repository

เปิด PowerShell และพิมพ์:

```powershell
cd c:\Users\LENOVO\Desktop\Meeting-room
```

- [ ] ✅ เข้าไปในโฟลเดอร์โปรเจคแล้ว

```powershell
git init
```

- [ ] ✅ เห็นข้อความ "Initialized empty Git repository"

```powershell
git add .
```

- [ ] ✅ เพิ่มไฟล์ทั้งหมดแล้ว

```powershell
git status
```

- [ ] ✅ เห็นรายการไฟล์สีเขียว
- [ ] ❌ **ไม่เห็น** `.env` (ต้องไม่มี!)
- [ ] ❌ **ไม่เห็น** `node_modules/` (ต้องไม่มี!)

```powershell
git commit -m "Initial commit: Meeting Room Booking System with Supabase"
```

- [ ] ✅ เห็นข้อความ commit สำเร็จ

```powershell
git branch -M main
```

- [ ] ✅ เปลี่ยนชื่อ branch เป็น main แล้ว

---

### ขั้นตอนที่ 6: เชื่อมต่อกับ GitHub

```powershell
git remote add origin https://github.com/Program-Phakanan/KWF-Project.git
```

- [ ] ✅ เชื่อมต่อกับ GitHub repository แล้ว

**ตรวจสอบ:**
```powershell
git remote -v
```

- [ ] ✅ เห็น URL ของ GitHub repository

---

### ขั้นตอนที่ 7: Push ขึ้น GitHub

**กรณีที่ 1: Repository ว่างเปล่า**

```powershell
git push -u origin main
```

**กรณีที่ 2: Repository มีข้อมูลอยู่แล้ว**

```powershell
git push -u origin main --force
```

**ระหว่าง Push:**
- [ ] เบราว์เซอร์เปิดขึ้นมา
- [ ] Login เข้า GitHub
- [ ] อนุญาตให้ Git เข้าถึง
- [ ] กลับมาที่ PowerShell
- [ ] รอให้ Upload เสร็จ
- [ ] ✅ เห็นข้อความ "Branch 'main' set up to track remote branch"

---

### ขั้นตอนที่ 8: ตรวจสอบบน GitHub

- [ ] เปิดเบราว์เซอร์ไปที่: **https://github.com/Program-Phakanan/KWF-Project**
- [ ] ✅ เห็นไฟล์ทั้งหมด
- [ ] ✅ เห็น `README.md` แสดงผลด้านล่าง
- [ ] ✅ เห็นโฟลเดอร์ `src/`, `public/`, `sql/`
- [ ] ✅ เห็นไฟล์ `.env.example`
- [ ] ❌ **ไม่เห็น** `.env` (ต้องไม่มี!)
- [ ] ❌ **ไม่เห็น** `node_modules/` (ต้องไม่มี!)

---

### ขั้นตอนที่ 9: ตรวจสอบ README

- [ ] คลิกที่ `README.md` บน GitHub
- [ ] ✅ เห็นเนื้อหาที่เขียนไว้
- [ ] ✅ แสดงผลถูกต้อง (มี emoji, headers, code blocks)
- [ ] ✅ มีคำอธิบายโปรเจค
- [ ] ✅ มีวิธีติดตั้ง
- [ ] ✅ มีวิธีใช้งาน

---

## 🎉 สำเร็จแล้ว!

### ✅ สิ่งที่ทำสำเร็จ

- [x] ติดตั้ง Git
- [x] ตั้งค่า Git
- [x] สร้าง Git Repository
- [x] เชื่อมต่อกับ GitHub
- [x] Push โค้ดขึ้น GitHub
- [x] ตรวจสอบบน GitHub
- [x] ✅ **โปรเจคอยู่บน GitHub แล้ว!**

---

## 🔄 การอัพเดทโค้ดในอนาคต

เมื่อแก้ไขโค้ดและต้องการ upload ใหม่:

```powershell
# 1. เพิ่มไฟล์ที่แก้ไข
git add .

# 2. Commit พร้อมข้อความ
git commit -m "อธิบายการเปลี่ยนแปลง"

# 3. Push ขึ้น GitHub
git push
```

**ตัวอย่าง:**
```powershell
git add .
git commit -m "แก้ไข: ปรับปรุง UI หน้า Login"
git push
```

---

## 🚨 ถ้ามีปัญหา

### ปัญหา: "git: command not found"
- [ ] ตรวจสอบว่าติดตั้ง Git แล้ว
- [ ] **รีสตาร์ท PowerShell**
- [ ] ลองพิมพ์ `git --version` อีกครั้ง

### ปัญหา: ".env ถูก upload ขึ้น GitHub"
```powershell
git rm --cached .env
git commit -m "Remove .env from repository"
git push
```

### ปัญหา: "Permission denied"
- [ ] ตรวจสอบว่า Login GitHub แล้ว
- [ ] ตรวจสอบว่า URL ถูกต้อง
- [ ] ลอง Login ใหม่

### ปัญหา: "Failed to push"
```powershell
git pull origin main --allow-unrelated-histories
git push -u origin main
```

---

## 📚 เอกสารที่เกี่ยวข้อง

- **FIX_SUMMARY.md** - สรุปการแก้ไขปัญหา
- **GITHUB_UPLOAD_GUIDE.md** - คู่มือละเอียด
- **QUICK_START.md** - คำสั่งย่อ
- **README.md** - เอกสารหลักโปรเจค

---

## 🎯 สรุป

### ✅ ที่ทำเสร็จแล้ว
1. ✅ แก้ปัญหารีเฟรชแล้วข้อมูลหาย
2. ✅ แก้ปัญหาออกจากระบบ
3. ✅ สร้างเอกสารครบถ้วน
4. ✅ เตรียมพร้อม Upload GitHub

### ⏳ ที่ต้องทำ
1. ⏳ ติดตั้ง Git (ถ้ายังไม่มี)
2. ⏳ Upload ขึ้น GitHub

### 🎉 เมื่อเสร็จแล้ว
- ✅ โปรเจคอยู่บน GitHub
- ✅ คนอื่นสามารถ clone ไปใช้ได้
- ✅ มีเอกสารครบถ้วน
- ✅ พร้อม Deploy ขึ้น Vercel

---

**วันที่:** 2026-01-28  
**สถานะ:** ✅ พร้อม Upload GitHub  
**ขั้นตอนถัดไป:** ทำตาม Checklist นี้ทีละขั้นตอน

---

**💡 เคล็ดลับ:** 
- ทำทีละขั้นตอน อย่าข้าม
- ตรวจสอบ ✅ ทุกข้อก่อนไปขั้นตอนถัดไป
- ถ้ามีปัญหา ดูที่ GITHUB_UPLOAD_GUIDE.md
